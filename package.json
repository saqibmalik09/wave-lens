{
  "name": "wave-lens",
  "version": "0.3.0",
  "private": true,
  "description": "Wave Lens monorepo — npm run deploy on the server (backend + dashboard + website).",
  "scripts": {
    "deploy": "bash deploy/deploy.sh",
    "deploy:backend": "bash deploy/deploy.sh backend",
    "deploy:dashboard": "bash deploy/deploy.sh dashboard",
    "deploy:website": "bash deploy/deploy.sh website"
  }
}
